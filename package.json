{
  "name": "jigglepeek-client",
  "description": "JigglePeek Windows Client",
  "author": "ceptoplex",
  "version": "0.1.4",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder build --win --publish never",
    "publish": "electron-builder build --win --publish always"
  },
  "devDependencies": {
    "electron": "^17.1.2",
    "electron-builder": "^22.14.13"
  },
  "dependencies": {
    "electron-updater": "^4.6.5"
  }
}
